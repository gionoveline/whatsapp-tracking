export default function DashboardLoading() {
  return (
    <main className="min-h-screen bg-[var(--background)] flex items-center justify-center p-8">
      <p className="text-[var(--muted-foreground)]">Carregando dashboard…</p>
    </main>
  );
}
